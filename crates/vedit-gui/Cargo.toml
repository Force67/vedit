[package]
name = "vedit-gui"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
iced = { version = "0.12", features = ["wgpu", "tokio"] }
rfd = "0.14"
vedit-core = { path = "../vedit-core" }
